@model DemoCCM.Models.Link[]

@{
    ViewBag.Title = "Save";
}
@{

    String space = "|";
    String enter = "\n";
}
@for (int i = 0; i < Model.Length; i++)
{
   @Model[i].ConceptID1 @space @Model[i].Text @space @Model[i].ConceptID2 @enter
}