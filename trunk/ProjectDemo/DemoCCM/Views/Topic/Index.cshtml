@model DemoCCM.Models.Map
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_DrawLayout.cshtml";
    String levelID3 = ViewBag.levelID2 as String;
    String topicID3 = ViewBag.topicID2 as String;
    
}
<div id="container">
	<header>

     <script>
             function allowDrop(ev) {
                 ev.preventDefault();
             }

             function drag(ev) {
                 ev.dataTransfer.setData("Text", ev.target.id);
             }

             function drop(ev) {
                 ev.preventDefault();
                 var data = ev.dataTransfer.getData("Text");
                 ev.target.appendChild(document.getElementById(data));
             }
     </script>
    <style type="text/css">
#div2 { width:350px;height:70px;padding:10px;border:1px solid #aaaaaa;}
</style>
		<div id="topbar">
    		<div id="toolbar">
                <img src="photos/logocc2.png" alt="Concept Map">
 
                      @{Html.RenderAction("_dropdownListPartial", "Topic",
                    new { LevelID = levelID3, topicId1 = topicID3 });}
               

            </div>
   		</div>
	</header>



 <div id="leftbar">  
    <div id="leftbox">
     concept
                  @foreach (var item in Model.Concepts)
                  {
                      <div id="drag1" draggable="true" ondragstart="drag(event)" width="336" height="69" >
                          @item.ConceptName @item.ConceptID</div>
                 
                  }

    </div>

    @{Html.RenderAction("_LinkOfTopicPartial", "Topic", new { links = Model.Links });}
</div>







    <div id="canvas-container">
            kkkkk
     </div>
    <footer>
    	<div id="bottombar"></div>
    </footer>
</div>

